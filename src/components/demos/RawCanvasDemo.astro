<script>
  const canvas = document.getElementById('raw-canvas') as HTMLCanvasElement | null;
  if (canvas) {
    const ctx = canvas.getContext('2d');

    if (ctx) {
      ctx.fillStyle = 'green';
      ctx.fillRect(10, 10, 150, 100);
    }
  }
</script>
